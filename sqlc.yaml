version: "2"
sql:
  - engine: "mysql"
    schema: "migrations" # directory is OK
    queries: "internal/queries" # directory of .sql query files
    gen:
      go:
        package: "queries"
        out: "generated/queries"
        sql_package: "database/sql"
        emit_interface: true
        emit_json_tags: true
